name: actions-security

on:
  pull_request:
    paths: [".github/**", "flake.nix", "flake.lock", "build.zig.zon"]
  push:
    branches: [main]
    paths: [".github/**", "flake.nix", "flake.lock", "build.zig.zon"]
  workflow_dispatch:

permissions: {}

jobs:
  zizmor:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Audit workflows
        uses: zizmorcore/zizmor-action@cc914d7f3750a2d13d75c7f184a1060aa0e9d482 # v0.6.4
        with:
          advanced-security: false
          min-confidence: medium

  pinact:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      actions: write # The Nix store cache saves development tools.
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          extra_nix_config: |
            access-tokens =
            accept-flake-config = false
      - name: Cache the Nix store
        uses: DeterminateSystems/magic-nix-cache-action@84c0677f58dcedf3b91f8223ce36a9ea5b3c84b7 # v15
        with:
          use-flakehub: false
      - name: Check workflow syntax
        run: nix develop --no-write-lock-file -c actionlint
      - name: Verify action pins
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: nix develop --no-write-lock-file -c pinact run --check --verify --min-age 3
